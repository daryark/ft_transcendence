{
  "name": "trans-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "React frontend for ft_transcendence (used by Docker image; paths assume /app + frontend/).",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "vite",
    "start:local": "vite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "vite": "^6.0.0"
  }
}
