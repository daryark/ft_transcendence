{
  "name": "trans-backend-server",
  "version": "1.0.0",
  "private": true,
  "description": "Express API for ft_transcendence (used by Docker image; paths assume /app + server/).",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "start:local": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
